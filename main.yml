# 强制覆盖所有逻辑，仅保留有效模式调用
case "$(date +%H%M)" in
  "0100") python fund_monitor.py --mode morning ;;  # UTC 1:00 = 北京时间9:00 早盘
  "0800") python fund_monitor.py --mode evening ;;  # UTC 8:00 = 北京时间16:00 复盘
  *) python fund_monitor.py --mode query ;;         # 所有其他时间 涨跌查询（彻底删除monitor/daily）
esac
